{
  "name": "front_1kr",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "install:all": "npm install --workspaces",
    "start": "npm run start --workspace backend",
    "dev": "npm run dev --workspace backend",
    "cert:windows": "npm run cert:windows --workspace backend"
  },
  "devDependencies": {}
}
